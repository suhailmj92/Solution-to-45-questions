let Name: string = "Suhail"; // This stores the name

console.log(Name.toLowerCase()); // prints the name in all lowercase letters

console.log(Name.toUpperCase()); // prints the name in all uppercase letters

console.log(Name.charAt(0).toUpperCase() + Name.slice(1).toLowerCase()); // prints the name with the first letter in uppercase